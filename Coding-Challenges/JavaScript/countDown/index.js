/* Count Down
Without changing the _starting_ value of `i`
create a function that returns an array of numbers
from 100 to 1 in descending order. 
Testing: `jest countDown/test.js`
*/

const countDown = () => {
  for (let i = 0; i; i) {}
};

module.exports = countDown;