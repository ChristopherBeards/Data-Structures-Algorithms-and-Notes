/* findVowels
This function should receive a string and return the number
of vowels in that string.
Example:
findVowels('aloha'); // -> 3
Testing: `jest findVowels/test.js`
*/

// function findVowels(str) {
//   const vowels = 'aeiou';
//   if (!str.length) return 0;
//   return (vowels.includes(str[0]) ? 1 : 0) + findVowels(str.slice(1));
// }

function findVowels(str) {}

module.exports = findVowels;
